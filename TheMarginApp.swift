import SwiftUI
import SwiftData

@main
struct TheMarginApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        // .modelContainer(for: [Project.self, Session.self, WritingTip.self])
        // Uncomment once models exist (Task 2)
    }
}
